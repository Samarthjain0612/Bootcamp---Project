#include <iostream>
using namespace std;
int main()
 {
    vector<int> gradingstudent (vector<int>v){
        int n=v.size();
        for (int i=0;i<n;i++){
            int qt=v[i]/5;
            int divf=qt*5;
            int nextf=(divf+5);
            if(divf<v[i]){
                int diff=(nrxtf-v[i]);
                if(diff<3&&nextf >=40)[
                    v[i]=(v[i]+diff);
                ]
            }
        }
    }
    return v;
 }
 int main()
 {
     oftream fourt getenv("OUTPUT_PATH");
 }
 